# Дана строка, содержащая полное имя файла, то есть имя диска, список каталогов (путь),
# собственно имя и расширение. Выделить из этой строки имя файла (без расширения)        

import os                                               # Делаем ввод и вывод в одну строку, и там же решение 
print(os.path.splitext(os.path.basename(input()))[0]    # Используя 'splitext' и 'basename' возвращаем базовое имя, разбиваем путь к файлу на кортеж, выводя первое его значение
